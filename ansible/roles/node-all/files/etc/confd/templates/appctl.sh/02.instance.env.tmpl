
flush /opt/app/bin/envs/instance.env << INSTANCE_ENV_EOF
declare -A ADDING_HOSTS
declare -A DELETING_HOSTS
ADDING_HOSTS=({{- range $joinN := lsdir "/adding-hosts" }}[{{ $joinN }}]="{{ range  getvs (printf "/adding-hosts/%s/*/instance_id" $joinN) }}{{ . }} {{ end }}" {{ end }})
DELETING_HOSTS=({{- range $delN := lsdir "/deleting-hosts" }}[{{ $delN }}]="{{ range  getvs (printf "/deleting-hosts/%s/*/instance_id" $delN) }}{{ . }} {{ end }}" {{ end }})

INSTANCE_ENV_EOF