
flush /opt/app/bin/envs/instance.env << INSTANCE_ENV_EOF

ADDING_HOSTS="{{- range $joinN := lsdir "/adding-hosts"  }}{{ range  getvs (printf "/adding-hosts/%s/*/instance_id" $joinN) }}{{ . }} {{ end }}{{ end }}"
DELETING_HOSTS="{{- range $delN := lsdir "/deleting-hosts"  }}{{ range  getvs (printf "/deleting-hosts/%s/*/instance_id" $delN) }}{{ . }} {{ end }}{{ end }}"

INSTANCE_ENV_EOF